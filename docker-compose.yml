# Optional local runtime for the public Nexus IQ app.
version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "7341:7341"
    environment:
      NXQ_PORT: "7341"
    command: npm run start
