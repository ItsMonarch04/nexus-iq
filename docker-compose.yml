# Optional local runtime for the public Nexus IQ app.
version: '3.8'
services:
  app:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NXQ_PORT: "7341"
    command: npm run start:docker
